{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Piattaforma Unitaria Functional Testing","text":"<p>Repository: <code>p4pa-pu-feature-test</code></p>"},{"location":"#features","title":"Features","text":"<ul> <li>Assessment storage from payments \u2014 3 scenario(s)</li> <li>Debt positions classification \u2014 9 scenario(s)</li> <li>Debt position creation \u2014 3 scenario(s)</li> <li>Debt positions import from a file \u2014 2 scenario(s)</li> <li>Receipts import from a file \u2014 2 scenario(s)</li> <li>Debt position notification via SEND \u2014 1 scenario(s)</li> <li>Mixed debt position management by SIL \u2014 2 scenario(s)</li> </ul>"},{"location":"assessment-storage-from-payments/","title":"Assessment storage from payments","text":"<p>Tags: <code>@assessments</code></p>"},{"location":"assessment-storage-from-payments/#scenario-an-assessment-detail-is-created-from-the-installment-balance-after-a-payment","title":"Scenario: An assessment detail is created from the installment balance after a payment","text":"<p>Tags: <code>@gpd</code></p> <pre><code>Given a simple debt position with balance created by organization interacting with GPD\n# (1)!\nWhen the citizen pays the installment of payment option 1\n# (2)!\nThen the receipt is processed correctly\n# (3)!\nAnd the debt position is in status paid\nAnd the assessment is in status closed\n# (4)!\nAnd the assessment detail is created correctly based on balance\n# (5)!\n</code></pre> <ol> <li>Like <code>a simple debt position created by organization...</code>, but with a balance set to the     installment; creates it and checks <code>UNPAID</code> status, presence in GPD/ACA and the scheduled expiration workflow.</li> <li> <p>Simulates the citizen paying the installment through the pagoPA node. It:</p> <ul> <li>verifies the payment notice (<code>verifyPaymentNotice</code>) and reads amount/due date;</li> <li>activates the payment notice (<code>activatePaymentNotice</code>) and gets a payment token;</li> <li>sends the payment outcome (<code>sendPaymentOutcome</code>), each call expected to return <code>OK</code></li> <li> <p>Checks that the pagoPA receipt has been fully processed. It verifies that:</p> </li> <li> <p>the receipt file <code>RT_&lt;nav&gt;.xml</code> reaches status <code>COMPLETED</code>;</p> </li> <li>the paid installment now has both <code>IUR</code> and <code>receiptId</code> set;</li> <li>the async <code>TRANSFER_CLASSIFICATION</code> workflow completes;</li> <li>the async <code>IUD_CLASSIFICATION</code> workflow completes.</li> <li> <p>Checks that the assessment for the paid installment is in the expected status. It:</p> </li> <li> <p>reads the installment and its <code>sourceFlowName</code>;</p> </li> <li>fetches the assessment by that name and asserts it exists and matches the status;</li> <li>checks the <code>CREATE_ASSESSMENT</code> workflow completes.</li> <li> <p>Checks that assessment details were created from the balance. It:</p> </li> <li> <p>retrieves the expected balance sections (optionally including a given DPTOBC section);</p> </li> <li>checks the <code>CLASSIFY_ASSESSMENT</code> workflow completes;</li> <li>verifies one assessment detail per section with matching IUR, <code>PAID</code> label, amount and registry codes.</li> </ul> </li> </ol>"},{"location":"assessment-storage-from-payments/#scenario-an-assessment-detail-is-created-from-the-debt-position-type-org-balance-after-a-payment","title":"Scenario: An assessment detail is created from the debt position type org balance after a payment","text":"<p>Tags: <code>@gpd</code></p> <pre><code>Given a simple debt position created by organization interacting with GPD\n# (1)!\nWhen the citizen pays the installment of payment option 1\n# (2)!\nThen the receipt is processed correctly\n# (3)!\nAnd the debt position is in status paid\nAnd the installment has balance field populated\nAnd the assessment is in status closed\n# (4)!\nAnd the assessment detail is created correctly based on balance\n# (5)!\n</code></pre> <ol> <li> <p>Creates a simple, single-installment debt position in one step. It:</p> <ul> <li>gets the organization token and builds the debt position entity;</li> <li>creates it and checks it becomes <code>UNPAID</code>;</li> <li>verifies the notice is present as <code>valid</code> in the GPD/ACA archive;</li> <li>checks the expiration workflow is scheduled.</li> <li> <p>Simulates the citizen paying the installment through the pagoPA node. It:</p> </li> <li> <p>verifies the payment notice (<code>verifyPaymentNotice</code>) and reads amount/due date;</p> </li> <li>activates the payment notice (<code>activatePaymentNotice</code>) and gets a payment token;</li> <li>sends the payment outcome (<code>sendPaymentOutcome</code>), each call expected to return <code>OK</code></li> <li> <p>Checks that the pagoPA receipt has been fully processed. It verifies that:</p> </li> <li> <p>the receipt file <code>RT_&lt;nav&gt;.xml</code> reaches status <code>COMPLETED</code>;</p> </li> <li>the paid installment now has both <code>IUR</code> and <code>receiptId</code> set;</li> <li>the async <code>TRANSFER_CLASSIFICATION</code> workflow completes;</li> <li>the async <code>IUD_CLASSIFICATION</code> workflow completes.</li> <li> <p>Checks that the assessment for the paid installment is in the expected status. It:</p> </li> <li> <p>reads the installment and its <code>sourceFlowName</code>;</p> </li> <li>fetches the assessment by that name and asserts it exists and matches the status;</li> <li>checks the <code>CREATE_ASSESSMENT</code> workflow completes.</li> <li> <p>Checks that assessment details were created from the balance. It:</p> </li> <li> <p>retrieves the expected balance sections (optionally including a given DPTOBC section);</p> </li> <li>checks the <code>CLASSIFY_ASSESSMENT</code> workflow completes;</li> <li>verifies one assessment detail per section with matching IUR, <code>PAID</code> label, amount and registry codes.</li> </ul> </li> </ol>"},{"location":"assessment-storage-from-payments/#scenario-an-assessment-detail-is-created-from-the-assessment-registry-after-a-payment","title":"Scenario: An assessment detail is created from the assessment registry after a payment","text":"<p>Tags: <code>@gpd</code></p> <pre><code>Given a simple debt position of type FEATURE_TEST_2 created by organization interacting with GPD\n# (1)!\nWhen the citizen pays the installment of payment option 1\n# (2)!\nThen the receipt is processed correctly\n# (3)!\nAnd the debt position is in status paid\nAnd the installment has balance field populated\nAnd the assessment is in status closed\n# (4)!\nAnd the assessment detail is created correctly based on balance\n# (5)!\n</code></pre> <ol> <li> <p>Creates a simple, single-installment debt position in one step. It:</p> <ul> <li>gets the organization token and builds the debt position entity;</li> <li>creates it and checks it becomes <code>UNPAID</code>;</li> <li>verifies the notice is present as <code>valid</code> in the GPD/ACA archive;</li> <li>checks the expiration workflow is scheduled.</li> <li> <p>Simulates the citizen paying the installment through the pagoPA node. It:</p> </li> <li> <p>verifies the payment notice (<code>verifyPaymentNotice</code>) and reads amount/due date;</p> </li> <li>activates the payment notice (<code>activatePaymentNotice</code>) and gets a payment token;</li> <li>sends the payment outcome (<code>sendPaymentOutcome</code>), each call expected to return <code>OK</code></li> <li> <p>Checks that the pagoPA receipt has been fully processed. It verifies that:</p> </li> <li> <p>the receipt file <code>RT_&lt;nav&gt;.xml</code> reaches status <code>COMPLETED</code>;</p> </li> <li>the paid installment now has both <code>IUR</code> and <code>receiptId</code> set;</li> <li>the async <code>TRANSFER_CLASSIFICATION</code> workflow completes;</li> <li>the async <code>IUD_CLASSIFICATION</code> workflow completes.</li> <li> <p>Checks that the assessment for the paid installment is in the expected status. It:</p> </li> <li> <p>reads the installment and its <code>sourceFlowName</code>;</p> </li> <li>fetches the assessment by that name and asserts it exists and matches the status;</li> <li>checks the <code>CREATE_ASSESSMENT</code> workflow completes.</li> <li> <p>Checks that assessment details were created from the balance. It:</p> </li> <li> <p>retrieves the expected balance sections (optionally including a given DPTOBC section);</p> </li> <li>checks the <code>CLASSIFY_ASSESSMENT</code> workflow completes;</li> <li>verifies one assessment detail per section with matching IUR, <code>PAID</code> label, amount and registry codes.</li> </ul> </li> </ol>"},{"location":"debt-position-creation/","title":"Debt position creation","text":"<p>Tags: <code>@debt_positions</code></p>"},{"location":"debt-position-creation/#scenario-a-simple-debt-position-is-created-by-an-organization-interacting-with-aca","title":"Scenario: A simple debt position is created by an organization interacting with ACA","text":"<p>Tags: <code>@aca</code></p> <pre><code>Given organization interacting with ACA\nAnd a new debt position of type FEATURE_TEST\nAnd payment option 1 with single installment of 100 euros with due date set in 2 days\nWhen the organization creates the debt position\n# (1)!\nThen the debt position is in status unpaid\nAnd the notice is present in ACA archive in status valid\nAnd the check of debt position expiration is scheduled\n</code></pre> <ol> <li> <p>Creates the debt position and validates the outcome. It:</p> <ul> <li>posts the debt position and asserts the call succeeds;</li> <li>validates the whole created structure (payment options, installments, first transfer) in <code>TO_SYNC</code>;</li> <li>checks the organization's sync workflow (ACA/GPD) completes.</li> </ul> </li> </ol>"},{"location":"debt-position-creation/#scenario-a-complex-debt-position-is-created-by-an-organization-interacting-with-aca","title":"Scenario: A complex debt position is created by an organization interacting with ACA","text":"<p>Tags: <code>@aca</code></p> <pre><code>Given organization interacting with ACA\nAnd a new debt position of type FEATURE_TEST\nAnd payment option 1 with 2 installments with due date set in 2 days\nAnd payment option 2 with 2 installments with due date set in 2 days\nWhen the organization creates the debt position\n# (1)!\nThen the debt position is in status unpaid\nAnd the notices are present in ACA archive in status valid\nAnd the check of debt position expiration is scheduled\n</code></pre> <ol> <li> <p>Creates the debt position and validates the outcome. It:</p> <ul> <li>posts the debt position and asserts the call succeeds;</li> <li>validates the whole created structure (payment options, installments, first transfer) in <code>TO_SYNC</code>;</li> <li>checks the organization's sync workflow (ACA/GPD) completes.</li> </ul> </li> </ol>"},{"location":"debt-position-creation/#scenario-a-simple-debt-position-is-created-by-an-organization-interacting-with-gpd","title":"Scenario: A simple debt position is created by an organization interacting with GPD","text":"<p>Tags: <code>@gpd</code></p> <pre><code>Given organization interacting with GPD\nAnd a new debt position of type FEATURE_TEST\nAnd payment option 1 with single installment of 100 euros with due date set in 2 days\nWhen the organization creates the debt position\n# (1)!\nThen the debt position is in status unpaid\nAnd the notice is present in GPD archive in status valid\nAnd the check of debt position expiration is scheduled\n</code></pre> <ol> <li> <p>Creates the debt position and validates the outcome. It:</p> <ul> <li>posts the debt position and asserts the call succeeds;</li> <li>validates the whole created structure (payment options, installments, first transfer) in <code>TO_SYNC</code>;</li> <li>checks the organization's sync workflow (ACA/GPD) completes.</li> </ul> </li> </ol>"},{"location":"debt-position-notification-via-send/","title":"Debt position notification via SEND","text":"<p>Tags: <code>@send</code></p>"},{"location":"debt-position-notification-via-send/#scenario-a-simple-debt-position-is-notified-by-send-and-when-it-is-paid-the-amount-is-updated","title":"Scenario: A simple debt position is notified by SEND and when it is paid the amount is updated","text":"<p>Tags: <code>@gpd</code></p> <pre><code>Given a simple debt position A for citizen X created by organization interacting with GPD\n# (1)!\nAnd a notification created for the single installment of debt position A\n# (2)!\nWhen the organization requires the notification to be uploaded to SEND\n# (3)!\nThen the notification is in status accepted and the IUN is assigned to the installment\n# (4)!\nAnd SEND has set a notification fee\n# (5)!\nWhen the citizen X pays the installment of debt position A\n# (6)!\nThen the receipt of debt position A is processed correctly\n# (7)!\nAnd the amount of installment of debt position A is increased by the notification fee\n# (8)!\nAnd the installment of debt position A has balance field populated\nAnd the assessment related to debt position A is in status closed\n# (9)!\nAnd the assessment detail is created correctly based on balance including notification cost section\n# (10)!\n</code></pre> <ol> <li> <p>Creates a simple, single-installment debt position in one step. It:</p> <ul> <li>gets the organization token and builds the debt position entity;</li> <li>creates it and checks it becomes <code>UNPAID</code>;</li> <li>verifies the notice is present as <code>valid</code> in the GPD/ACA archive;</li> <li>checks the expiration workflow is scheduled.</li> <li> <p>Creates a SEND notification for the selected installments. It:</p> </li> <li> <p>builds the notification request (recipients and pagoPA payments) for the given debt positions;</p> </li> <li>gets a SEND token and posts the notification;</li> <li>asserts the call succeeds and the notification is created in status <code>WAITING</code>.</li> <li> <p>Uploads the notification and payment PDFs to SEND. It:</p> </li> <li> <p>uploads the notification file and one payment file per notified installment;</p> </li> <li>reads the workflow id from the last upload;</li> <li>checks the notification moves to <code>IN_VALIDATION</code> with no IUN yet;</li> <li>waits for the upload workflow to complete.</li> <li> <p>Checks that the notification is valid and the IUN is assigned. It:</p> </li> <li> <p>polls SEND until the notification is valid and asserts it has an IUN;</p> </li> <li>verifies that every notified installment carries that same IUN.</li> <li>Checks that SEND set a notification fee: reads the fee for each notified installment, asserts it is present and that all installments share the same fee.</li> <li> <p>Simulates the citizen paying the installment through the pagoPA node. It:</p> </li> <li> <p>verifies the payment notice (<code>verifyPaymentNotice</code>) and reads amount/due date;</p> </li> <li>activates the payment notice (<code>activatePaymentNotice</code>) and gets a payment token;</li> <li>sends the payment outcome (<code>sendPaymentOutcome</code>), each call expected to return <code>OK</code></li> <li> <p>Checks that the pagoPA receipt has been fully processed. It verifies that:</p> </li> <li> <p>the receipt file <code>RT_&lt;nav&gt;.xml</code> reaches status <code>COMPLETED</code>;</p> </li> <li>the paid installment now has both <code>IUR</code> and <code>receiptId</code> set;</li> <li>the async <code>TRANSFER_CLASSIFICATION</code> workflow completes;</li> <li>the async <code>IUD_CLASSIFICATION</code> workflow completes.</li> <li>Checks that the notified installment's amount was increased by the notification fee (both <code>notificationFeeCents</code> and <code>amountCents</code>).</li> <li> <p>Checks that the assessment for the paid installment is in the expected status. It:</p> </li> <li> <p>reads the installment and its <code>sourceFlowName</code>;</p> </li> <li>fetches the assessment by that name and asserts it exists and matches the status;</li> <li>checks the <code>CREATE_ASSESSMENT</code> workflow completes.</li> <li> <p>Checks that assessment details were created from the balance. It:</p> </li> <li> <p>retrieves the expected balance sections (optionally including a given DPTOBC section);</p> </li> <li>checks the <code>CLASSIFY_ASSESSMENT</code> workflow completes;</li> <li>verifies one assessment detail per section with matching IUR, <code>PAID</code> label, amount and registry codes.</li> </ul> </li> </ol>"},{"location":"debt-positions-classification/","title":"Debt positions classification","text":"<p>Tags: <code>@classification</code></p>"},{"location":"debt-positions-classification/#scenario-outline-a-simple-debt-position-created-on-is-paid-after-a-citizen-payment","title":"Scenario Outline: A simple debt position created on  is paid after a citizen payment <p>Tags: <code>@payment</code></p> <pre><code>Given a simple debt position created by organization interacting with &lt;interaction&gt;\n# (1)!\nWhen the citizen pays the installment of payment option 1\n# (2)!\nThen the receipt is processed correctly\n# (3)!\nAnd the debt position is in status paid\nAnd the check of debt position expiration is canceled\n</code></pre> <ol> <li> <p>Creates a simple, single-installment debt position in one step. It:</p> <ul> <li>gets the organization token and builds the debt position entity;</li> <li>creates it and checks it becomes <code>UNPAID</code>;</li> <li>verifies the notice is present as <code>valid</code> in the GPD/ACA archive;</li> <li>checks the expiration workflow is scheduled.</li> <li> <p>Simulates the citizen paying the installment through the pagoPA node. It:</p> </li> <li> <p>verifies the payment notice (<code>verifyPaymentNotice</code>) and reads amount/due date;</p> </li> <li>activates the payment notice (<code>activatePaymentNotice</code>) and gets a payment token;</li> <li>sends the payment outcome (<code>sendPaymentOutcome</code>), each call expected to return <code>OK</code></li> <li> <p>Checks that the pagoPA receipt has been fully processed. It verifies that:</p> </li> <li> <p>the receipt file <code>RT_&lt;nav&gt;.xml</code> reaches status <code>COMPLETED</code>;</p> </li> <li>the paid installment now has both <code>IUR</code> and <code>receiptId</code> set;</li> <li>the async <code>TRANSFER_CLASSIFICATION</code> workflow completes;</li> <li>the async <code>IUD_CLASSIFICATION</code> workflow completes.</li> </ul> </li> </ol> <p>Examples</p>    interaction     ACA    <p>Examples</p>    interaction     GPD","text":""},{"location":"debt-positions-classification/#scenario-outline-a-simple-debt-position-created-on-is-reported-after-payment-payment-reporting-and-treasury","title":"Scenario Outline: A simple debt position created on  is reported after payment, payment reporting and treasury <pre><code>Given a simple debt position created by organization interacting with &lt;interaction&gt;\n# (1)!\nWhen the citizen pays the installment of payment option 1\n# (2)!\nThen the receipt is processed correctly\n# (3)!\nAnd the debt position is in status paid\nAnd the check of debt position expiration is canceled\nAnd the classification labels are RT_NO_IUF, RT_NO_IUD\n# (4)!\nWhen the organization uploads the payment reporting file about installment of payment option 1\nThen the payment reporting is processed correctly\n# (5)!\nAnd the debt position is in status reported\nAnd the classification labels are RT_IUF, IUF_NO_TES, RT_NO_IUD\n# (6)!\nWhen the organization uploads the treasury file with amount of 100 euros\nThen the treasury is processed correctly\n# (7)!\nAnd the debt position is in status reported\nAnd the classification labels are RT_IUF, RT_IUF_TES, RT_NO_IUD\n# (8)!\n</code></pre> <ol> <li> <p>Creates a simple, single-installment debt position in one step. It:</p> <ul> <li>gets the organization token and builds the debt position entity;</li> <li>creates it and checks it becomes <code>UNPAID</code>;</li> <li>verifies the notice is present as <code>valid</code> in the GPD/ACA archive;</li> <li>checks the expiration workflow is scheduled.</li> <li> <p>Simulates the citizen paying the installment through the pagoPA node. It:</p> </li> <li> <p>verifies the payment notice (<code>verifyPaymentNotice</code>) and reads amount/due date;</p> </li> <li>activates the payment notice (<code>activatePaymentNotice</code>) and gets a payment token;</li> <li>sends the payment outcome (<code>sendPaymentOutcome</code>), each call expected to return <code>OK</code></li> <li> <p>Checks that the pagoPA receipt has been fully processed. It verifies that:</p> </li> <li> <p>the receipt file <code>RT_&lt;nav&gt;.xml</code> reaches status <code>COMPLETED</code>;</p> </li> <li>the paid installment now has both <code>IUR</code> and <code>receiptId</code> set;</li> <li>the async <code>TRANSFER_CLASSIFICATION</code> workflow completes;</li> <li>the async <code>IUD_CLASSIFICATION</code> workflow completes.</li> <li> <p>Checks the classification labels produced for the installment. It:</p> </li> <li> <p>queries the classifications for today by IUV (and by IUF/IUR when payment reporting with outcome code specified is uploaded);</p> </li> <li>asserts at least one classification exists;</li> <li>verifies that, for every transfer, the stored labels match the expected set.</li> <li> <p>Checks that the payment reporting file was processed correctly. It verifies that:</p> </li> <li> <p>the file reaches status <code>COMPLETED</code>;</p> </li> <li>the <code>PAYMENTS_REPORTING_INGESTION</code> workflow completes;</li> <li>the <code>TRANSFER_CLASSIFICATION</code> workflow completes for each transfer;</li> <li>the installment now has its <code>IUF</code> set.</li> <li> <p>Checks the classification labels produced for the installment. It:</p> </li> <li> <p>queries the classifications for today by IUV (and by IUF/IUR when payment reporting with outcome code specified is uploaded);</p> </li> <li>asserts at least one classification exists;</li> <li>verifies that, for every transfer, the stored labels match the expected set.</li> <li> <p>Checks that the treasury OPI file was processed correctly. It verifies that:</p> </li> <li> <p>the file reaches status <code>COMPLETED</code>;</p> </li> <li>the <code>TREASURY_OPI_INGESTION</code> workflow completes;</li> <li>the <code>TRANSFER_CLASSIFICATION</code> workflow completes for each transfer;</li> <li>the <code>IUF_CLASSIFICATION</code> workflow completes.</li> <li> <p>Checks the classification labels produced for the installment. It:</p> </li> <li> <p>queries the classifications for today by IUV (and by IUF/IUR when payment reporting with outcome code specified is uploaded);</p> </li> <li>asserts at least one classification exists;</li> <li>verifies that, for every transfer, the stored labels match the expected set.</li> </ul> </li> </ol> <p>Examples</p>    interaction     ACA    <p>Examples</p>    interaction     GPD","text":""},{"location":"debt-positions-classification/#scenario-a-complex-debt-position-created-on-gpd-is-partially-paid-after-payment-payment-reporting-and-treasury-of-one-installment","title":"Scenario: A complex debt position created on GPD is partially paid after payment, payment reporting and treasury of one installment <p>Tags: <code>@gpd</code></p> <pre><code>Given a complex debt position with 2 payment options created by organization interacting with GPD\n# (1)!\nWhen the citizen pays the installment 1 of payment option 1\n# (2)!\nThen the receipt is processed correctly\n# (3)!\nAnd the installment 1 of payment option 1 is in status paid\nAnd the payment option 1 is in status partially_paid\nAnd the debt position is in status partially_paid\nAnd the payment option 2 is in status invalid\nAnd the classification labels are RT_NO_IUF, RT_NO_IUD\n# (4)!\nWhen the organization uploads the payment reporting file about installment 1 of payment option 1\nThen the payment reporting is processed correctly\n# (5)!\nAnd the installment 1 of payment option 1 is in status reported\nAnd the payment option 1 is in status partially_paid\nAnd the debt position is in status partially_paid\nAnd the classification labels are RT_NO_IUD, RT_IUF, IUF_NO_TES\n# (6)!\nWhen the organization uploads the treasury file with amount of installment 1 of payment option 1\nThen the treasury is processed correctly\n# (7)!\nAnd the payment option 1 is in status partially_paid\nAnd the debt position is in status partially_paid\nAnd the classification labels are RT_NO_IUD, RT_IUF, RT_IUF_TES\n# (8)!\n</code></pre> <ol> <li> <p>Creates a complex debt position (several payment options with installments) in one step. It:</p> <ul> <li>gets the organization token and builds the payment options;</li> <li>creates the debt position and checks it becomes <code>UNPAID</code>;</li> <li>verifies presence in the GPD/ACA archive and the scheduled expiration workflow.</li> <li> <p>Simulates the citizen paying the installment through the pagoPA node. It:</p> </li> <li> <p>verifies the payment notice (<code>verifyPaymentNotice</code>) and reads amount/due date;</p> </li> <li>activates the payment notice (<code>activatePaymentNotice</code>) and gets a payment token;</li> <li>sends the payment outcome (<code>sendPaymentOutcome</code>), each call expected to return <code>OK</code></li> <li> <p>Checks that the pagoPA receipt has been fully processed. It verifies that:</p> </li> <li> <p>the receipt file <code>RT_&lt;nav&gt;.xml</code> reaches status <code>COMPLETED</code>;</p> </li> <li>the paid installment now has both <code>IUR</code> and <code>receiptId</code> set;</li> <li>the async <code>TRANSFER_CLASSIFICATION</code> workflow completes;</li> <li>the async <code>IUD_CLASSIFICATION</code> workflow completes.</li> <li> <p>Checks the classification labels produced for the installment. It:</p> </li> <li> <p>queries the classifications for today by IUV (and by IUF/IUR when payment reporting with outcome code specified is uploaded);</p> </li> <li>asserts at least one classification exists;</li> <li>verifies that, for every transfer, the stored labels match the expected set.</li> <li> <p>Checks that the payment reporting file was processed correctly. It verifies that:</p> </li> <li> <p>the file reaches status <code>COMPLETED</code>;</p> </li> <li>the <code>PAYMENTS_REPORTING_INGESTION</code> workflow completes;</li> <li>the <code>TRANSFER_CLASSIFICATION</code> workflow completes for each transfer;</li> <li>the installment now has its <code>IUF</code> set.</li> <li> <p>Checks the classification labels produced for the installment. It:</p> </li> <li> <p>queries the classifications for today by IUV (and by IUF/IUR when payment reporting with outcome code specified is uploaded);</p> </li> <li>asserts at least one classification exists;</li> <li>verifies that, for every transfer, the stored labels match the expected set.</li> <li> <p>Checks that the treasury OPI file was processed correctly. It verifies that:</p> </li> <li> <p>the file reaches status <code>COMPLETED</code>;</p> </li> <li>the <code>TREASURY_OPI_INGESTION</code> workflow completes;</li> <li>the <code>TRANSFER_CLASSIFICATION</code> workflow completes for each transfer;</li> <li>the <code>IUF_CLASSIFICATION</code> workflow completes.</li> <li> <p>Checks the classification labels produced for the installment. It:</p> </li> <li> <p>queries the classifications for today by IUV (and by IUF/IUR when payment reporting with outcome code specified is uploaded);</p> </li> <li>asserts at least one classification exists;</li> <li>verifies that, for every transfer, the stored labels match the expected set.</li> </ul> </li> </ol>","text":""},{"location":"debt-positions-classification/#scenario-a-complex-debt-position-created-on-gpd-is-reported-after-payment-payment-reporting-and-treasury-of-all-installments","title":"Scenario: A complex debt position created on GPD is reported after payment, payment reporting and treasury of all installments <p>Tags: <code>@gpd</code></p> <pre><code>Given a complex debt position with 2 payment options created by organization interacting with GPD\n# (1)!\nAnd the previous payment of installment 1 of payment option 1\n# (2)!\nWhen the citizen pays the installment 2 of payment option 1\n# (3)!\nThen the receipt is processed correctly\n# (4)!\nAnd the installment 2 of payment option 1 is in status paid\nAnd the payment option 1 is in status paid\nAnd the debt position is in status paid\nAnd the classification labels are RT_NO_IUF, RT_NO_IUD\n# (5)!\nWhen the organization uploads the payment reporting file about installment 2 of payment option 1\nThen the payment reporting is processed correctly\n# (6)!\nAnd the installment 2 of payment option 1 is in status reported\nAnd the payment option 1 is in status reported\nAnd the debt position is in status reported\nAnd the classification labels are RT_NO_IUD, RT_IUF, IUF_NO_TES\n# (7)!\nWhen the organization uploads the treasury file with amount of installment 2 of payment option 1\nThen the treasury is processed correctly\n# (8)!\nAnd the payment option 1 is in status reported\nAnd the debt position is in status reported\nAnd the classification labels are RT_NO_IUD, RT_IUF, RT_IUF_TES\n# (9)!\n</code></pre> <ol> <li> <p>Creates a complex debt position (several payment options with installments) in one step. It:</p> <ul> <li>gets the organization token and builds the payment options;</li> <li>creates the debt position and checks it becomes <code>UNPAID</code>;</li> <li>verifies presence in the GPD/ACA archive and the scheduled expiration workflow.</li> <li> <p>Given that plays a full payment-and-reporting cycle for one installment and checks the outcome:</p> </li> <li> <p>pays the installment and checks the receipt is processed;</p> </li> <li>checks the other payment options become <code>INVALID</code>;</li> <li>uploads the payment reporting and checks the installment becomes <code>REPORTED</code>;</li> <li>uploads the treasury file and checks it is processed;</li> <li>checks the payment option and debt position become <code>PARTIALLY_PAID</code>;</li> <li>checks the resulting classification labels.</li> <li> <p>Simulates the citizen paying the installment through the pagoPA node. It:</p> </li> <li> <p>verifies the payment notice (<code>verifyPaymentNotice</code>) and reads amount/due date;</p> </li> <li>activates the payment notice (<code>activatePaymentNotice</code>) and gets a payment token;</li> <li>sends the payment outcome (<code>sendPaymentOutcome</code>), each call expected to return <code>OK</code></li> <li> <p>Checks that the pagoPA receipt has been fully processed. It verifies that:</p> </li> <li> <p>the receipt file <code>RT_&lt;nav&gt;.xml</code> reaches status <code>COMPLETED</code>;</p> </li> <li>the paid installment now has both <code>IUR</code> and <code>receiptId</code> set;</li> <li>the async <code>TRANSFER_CLASSIFICATION</code> workflow completes;</li> <li>the async <code>IUD_CLASSIFICATION</code> workflow completes.</li> <li> <p>Checks the classification labels produced for the installment. It:</p> </li> <li> <p>queries the classifications for today by IUV (and by IUF/IUR when payment reporting with outcome code specified is uploaded);</p> </li> <li>asserts at least one classification exists;</li> <li>verifies that, for every transfer, the stored labels match the expected set.</li> <li> <p>Checks that the payment reporting file was processed correctly. It verifies that:</p> </li> <li> <p>the file reaches status <code>COMPLETED</code>;</p> </li> <li>the <code>PAYMENTS_REPORTING_INGESTION</code> workflow completes;</li> <li>the <code>TRANSFER_CLASSIFICATION</code> workflow completes for each transfer;</li> <li>the installment now has its <code>IUF</code> set.</li> <li> <p>Checks the classification labels produced for the installment. It:</p> </li> <li> <p>queries the classifications for today by IUV (and by IUF/IUR when payment reporting with outcome code specified is uploaded);</p> </li> <li>asserts at least one classification exists;</li> <li>verifies that, for every transfer, the stored labels match the expected set.</li> <li> <p>Checks that the treasury OPI file was processed correctly. It verifies that:</p> </li> <li> <p>the file reaches status <code>COMPLETED</code>;</p> </li> <li>the <code>TREASURY_OPI_INGESTION</code> workflow completes;</li> <li>the <code>TRANSFER_CLASSIFICATION</code> workflow completes for each transfer;</li> <li>the <code>IUF_CLASSIFICATION</code> workflow completes.</li> <li> <p>Checks the classification labels produced for the installment. It:</p> </li> <li> <p>queries the classifications for today by IUV (and by IUF/IUR when payment reporting with outcome code specified is uploaded);</p> </li> <li>asserts at least one classification exists;</li> <li>verifies that, for every transfer, the stored labels match the expected set.</li> </ul> </li> </ol>","text":""},{"location":"debt-positions-classification/#scenario-a-debt-position-created-on-gpd-gets-a-receipt-and-is-reported-after-payment-reporting-with-outcome-code-9","title":"Scenario: A debt position created on GPD gets a receipt and is reported after payment reporting with outcome code 9 <p>Tags: <code>@classification_outcome9</code> <code>@gpd</code></p> <pre><code>Given a simple debt position created by organization interacting with GPD\n# (1)!\nWhen the organization uploads the payment reporting file about installment of payment option 1 with outcome code 9\nThen the payment reporting with outcome code 9 is processed correctly\n# (2)!\nAnd the receipt is created correctly with origin payments_reporting\n# (3)!\nAnd the installment of payment option 1 is in status reported\nAnd the debt position is in status reported\nAnd the classification labels are IUF_NO_TES, RT_IUF, RT_NO_IUD\n# (4)!\n</code></pre> <ol> <li> <p>Creates a simple, single-installment debt position in one step. It:</p> <ul> <li>gets the organization token and builds the debt position entity;</li> <li>creates it and checks it becomes <code>UNPAID</code>;</li> <li>verifies the notice is present as <code>valid</code> in the GPD/ACA archive;</li> <li>checks the expiration workflow is scheduled.</li> <li> <p>Checks that a payment reporting with outcome code 9 was processed correctly. It verifies that:</p> </li> <li> <p>the file reaches status <code>COMPLETED</code>;</p> </li> <li>the <code>PAYMENTS_REPORTING_INGESTION</code> workflow completes;</li> <li>the <code>TRANSFER_CLASSIFICATION</code> workflow completes.</li> <li> <p>Checks that the receipt for the paid installment was created with the expected origin. It verifies that:</p> </li> <li> <p>the receipt exists (looked up by IUV/IUR, or by IUR when it is a duplicate);</p> </li> <li>its <code>receiptOrigin</code> matches the expected one and it is linked to the installment (<code>receiptId</code>);</li> <li>the <code>TRANSFER_CLASSIFICATION</code> workflow completes for each transfer;</li> <li>the <code>IUD_CLASSIFICATION</code> workflow completes.</li> <li> <p>Checks the classification labels produced for the installment. It:</p> </li> <li> <p>queries the classifications for today by IUV (and by IUF/IUR when payment reporting with outcome code specified is uploaded);</p> </li> <li>asserts at least one classification exists;</li> <li>verifies that, for every transfer, the stored labels match the expected set.</li> </ul> </li> </ol>","text":""},{"location":"debt-positions-classification/#scenario-a-debt-position-created-outside-pu-gets-a-receipt-and-is-reported-after-payment-reporting-with-outcome-code-9","title":"Scenario: A debt position created outside PU gets a receipt and is reported after payment reporting with outcome code 9 <p>Tags: <code>@classification_outcome9</code> <code>@gpd</code></p> <pre><code>Given organization interacting with GPD\nWhen the organization uploads the payment reporting file with outcome code 9 about a debt position created outside PU\nThen the payment reporting with outcome code 9 is processed correctly\n# (1)!\nAnd the debt position is created correctly\n# (2)!\nAnd the receipt is created correctly with origin payments_reporting\n# (3)!\nAnd the installment of the created debt position is in status reported\nAnd the debt position is in status reported\nAnd the classification labels are IUF_NO_TES, RT_IUF, RT_NO_IUD\n# (4)!\n</code></pre> <ol> <li> <p>Checks that a payment reporting with outcome code 9 was processed correctly. It verifies that:</p> <ul> <li>the file reaches status <code>COMPLETED</code>;</li> <li>the <code>PAYMENTS_REPORTING_INGESTION</code> workflow completes;</li> <li>the <code>TRANSFER_CLASSIFICATION</code> workflow completes.</li> <li> <p>Checks that the debt position was created correctly. It:</p> </li> <li> <p>looks it up by NAV and asserts exactly one is found;</p> </li> <li>verifies its origin and, for receipt-file origin, that it is <code>PAID</code>;</li> <li>checks the installment IUV and, for pagopa-reporting origin, the outcome-9 remittance and anonymous debtor.</li> <li> <p>Checks that the receipt for the paid installment was created with the expected origin. It verifies that:</p> </li> <li> <p>the receipt exists (looked up by IUV/IUR, or by IUR when it is a duplicate);</p> </li> <li>its <code>receiptOrigin</code> matches the expected one and it is linked to the installment (<code>receiptId</code>);</li> <li>the <code>TRANSFER_CLASSIFICATION</code> workflow completes for each transfer;</li> <li>the <code>IUD_CLASSIFICATION</code> workflow completes.</li> <li> <p>Checks the classification labels produced for the installment. It:</p> </li> <li> <p>queries the classifications for today by IUV (and by IUF/IUR when payment reporting with outcome code specified is uploaded);</p> </li> <li>asserts at least one classification exists;</li> <li>verifies that, for every transfer, the stored labels match the expected set.</li> </ul> </li> </ol>","text":""},{"location":"debt-positions-classification/#scenario-an-installment-already-paid-and-reported-is-classified-as-duplicate-doppi-when-a-second-payment-reporting-with-outcome-code-9-arrives","title":"Scenario: An installment already paid and reported is classified as duplicate (DOPPI) when a second payment reporting with outcome code 9 arrives <p>Tags: <code>@classification_duplicates</code> <code>@gpd</code></p> <pre><code>Given a simple debt position created by organization interacting with GPD\n# (1)!\nAnd the successful payment of the installment\n# (2)!\nAnd a payment reporting with outcome code 0 has been successfully processed for the installment\n# (3)!\nWhen the organization uploads a second payment reporting for the installment with outcome code 9\nThen the payment reporting with outcome code 9 is processed correctly\n# (4)!\nAnd the duplicate receipt for the payment reporting with outcome code 9 is created correctly with origin payments_reporting\n# (5)!\nAnd the debt position is in status reported\nAnd the classification labels related to payment reporting with outcome code 0 are IUF_NO_TES, RT_IUF, RT_NO_IUD, DOPPI\n# (6)!\nAnd the classification labels related to payment reporting with outcome code 9 are IUF_NO_TES, IUV_NO_RT, DOPPI\n# (7)!\n</code></pre> <ol> <li> <p>Creates a simple, single-installment debt position in one step. It:</p> <ul> <li>gets the organization token and builds the debt position entity;</li> <li>creates it and checks it becomes <code>UNPAID</code>;</li> <li>verifies the notice is present as <code>valid</code> in the GPD/ACA archive;</li> <li>checks the expiration workflow is scheduled.</li> <li> <p>Performs a full successful payment in one step:</p> </li> <li> <p>pays the installment,</p> </li> <li>checks the debt position becomes <code>PAID</code></li> <li>checks the receipt is processed correctly.</li> <li> <p>Uploads a payment reporting and checks it is processed, in one step:</p> </li> <li> <p>uploads the file,</p> </li> <li>checks the debt position becomes <code>REPORTED</code>,</li> <li>checks processing and (for outcome 9) that the receipt is created.</li> <li> <p>Checks that a payment reporting with outcome code 9 was processed correctly. It verifies that:</p> </li> <li> <p>the file reaches status <code>COMPLETED</code>;</p> </li> <li>the <code>PAYMENTS_REPORTING_INGESTION</code> workflow completes;</li> <li>the <code>TRANSFER_CLASSIFICATION</code> workflow completes.</li> <li>Checks that the duplicate receipt for the given payment-reporting flow (by outcome code) is created with the expected origin.</li> <li> <p>Checks the classification labels produced for the installment. It:</p> </li> <li> <p>queries the classifications for today by IUV (and by IUF/IUR when payment reporting with outcome code specified is uploaded);</p> </li> <li>asserts at least one classification exists;</li> <li>verifies that, for every transfer, the stored labels match the expected set.</li> <li> <p>Checks the classification labels produced for the installment. It:</p> </li> <li> <p>queries the classifications for today by IUV (and by IUF/IUR when payment reporting with outcome code specified is uploaded);</p> </li> <li>asserts at least one classification exists;</li> <li>verifies that, for every transfer, the stored labels match the expected set.</li> </ul> </li> </ol>","text":""},{"location":"debt-positions-classification/#scenario-an-installment-is-classified-as-duplicate-doppi-when-a-payment-reporting-with-outcome-code-9-arrives-before-the-payment-and-it-is-later-reported","title":"Scenario: An installment is classified as duplicate (DOPPI) when a payment reporting with outcome code 9 arrives before the payment and it is later reported <p>Tags: <code>@classification_duplicates</code> <code>@gpd</code></p> <pre><code>Given a simple debt position created by organization interacting with GPD\n# (1)!\nAnd a payment reporting with outcome code 9 has been successfully processed for the installment\n# (2)!\nAnd the successful payment of the installment\n# (3)!\nWhen the organization uploads a second payment reporting for the installment with outcome code 0\nThen the payment reporting is processed correctly\n# (4)!\nAnd the debt position is in status reported\nAnd the classification labels related to payment reporting with outcome code 9 are IUF_NO_TES, RT_IUF, RT_NO_IUD, DOPPI\n# (5)!\nAnd the classification labels related to payment reporting with outcome code 0 are IUF_NO_TES, RT_IUF, RT_NO_IUD, DOPPI\n# (6)!\n</code></pre> <ol> <li> <p>Creates a simple, single-installment debt position in one step. It:</p> <ul> <li>gets the organization token and builds the debt position entity;</li> <li>creates it and checks it becomes <code>UNPAID</code>;</li> <li>verifies the notice is present as <code>valid</code> in the GPD/ACA archive;</li> <li>checks the expiration workflow is scheduled.</li> <li> <p>Uploads a payment reporting and checks it is processed, in one step:</p> </li> <li> <p>uploads the file,</p> </li> <li>checks the debt position becomes <code>REPORTED</code>,</li> <li>checks processing and (for outcome 9) that the receipt is created.</li> <li> <p>Performs a full successful payment in one step:</p> </li> <li> <p>pays the installment,</p> </li> <li>checks the debt position becomes <code>PAID</code></li> <li>checks the receipt is processed correctly.</li> <li> <p>Checks that the payment reporting file was processed correctly. It verifies that:</p> </li> <li> <p>the file reaches status <code>COMPLETED</code>;</p> </li> <li>the <code>PAYMENTS_REPORTING_INGESTION</code> workflow completes;</li> <li>the <code>TRANSFER_CLASSIFICATION</code> workflow completes for each transfer;</li> <li>the installment now has its <code>IUF</code> set.</li> <li> <p>Checks the classification labels produced for the installment. It:</p> </li> <li> <p>queries the classifications for today by IUV (and by IUF/IUR when payment reporting with outcome code specified is uploaded);</p> </li> <li>asserts at least one classification exists;</li> <li>verifies that, for every transfer, the stored labels match the expected set.</li> <li> <p>Checks the classification labels produced for the installment. It:</p> </li> <li> <p>queries the classifications for today by IUV (and by IUF/IUR when payment reporting with outcome code specified is uploaded);</p> </li> <li>asserts at least one classification exists;</li> <li>verifies that, for every transfer, the stored labels match the expected set.</li> </ul> </li> </ol>","text":""},{"location":"debt-positions-classification/#scenario-an-installment-created-outside-pu-is-classified-as-duplicate-doppi-when-a-payment-reporting-with-outcome-code-9-arrives-before-the-payment-and-it-is-later-reported","title":"Scenario: An installment, created outside PU, is classified as duplicate (DOPPI) when a payment reporting with outcome code 9 arrives before the payment and it is later reported <p>Tags: <code>@classification_duplicates</code> <code>@gpd</code></p> <pre><code>Given a simple debt position created on GPD\n# (1)!\nAnd a payment reporting with outcome code 9 has been successfully processed for the installment created outside PU\n# (2)!\nAnd the successful payment of the installment created outside PU\n# (3)!\nWhen the organization uploads a second payment reporting for the installment created outside PU with outcome code 0\nThen the payment reporting is processed correctly\n# (4)!\nAnd the classification labels related to payment reporting with outcome code 9 are IUF_NO_TES, RT_IUF, RT_NO_IUD, DOPPI\n# (5)!\nAnd the classification labels related to payment reporting with outcome code 0 are IUF_NO_TES, RT_IUF, RT_NO_IUD, DOPPI\n# (6)!\n</code></pre> <ol> <li> <p>Creates a simple debt position directly on GPD/ACA (bypassing PU creation):</p> <ul> <li>builds the entity and its transfer, generating IUV/NAV/IUPD;</li> <li>posts it to GPD/ACA and asserts the call succeeds;</li> <li>verifies the notice is present as <code>VALID</code> in the GPD/ACA archive.</li> <li>For a debt position created outside PU, uploads a payment reporting with outcome code 9 and checks, in one step, that it is processed and that the debt position is created correctly.</li> <li>Like <code>the successful payment of the installment</code>, but pays the first installment of a debt position created outside PU, then checks the <code>PAID</code> status and that the receipt is processed.</li> <li> <p>Checks that the payment reporting file was processed correctly. It verifies that:</p> </li> <li> <p>the file reaches status <code>COMPLETED</code>;</p> </li> <li>the <code>PAYMENTS_REPORTING_INGESTION</code> workflow completes;</li> <li>the <code>TRANSFER_CLASSIFICATION</code> workflow completes for each transfer;</li> <li>the installment now has its <code>IUF</code> set.</li> <li> <p>Checks the classification labels produced for the installment. It:</p> </li> <li> <p>queries the classifications for today by IUV (and by IUF/IUR when payment reporting with outcome code specified is uploaded);</p> </li> <li>asserts at least one classification exists;</li> <li>verifies that, for every transfer, the stored labels match the expected set.</li> <li> <p>Checks the classification labels produced for the installment. It:</p> </li> <li> <p>queries the classifications for today by IUV (and by IUF/IUR when payment reporting with outcome code specified is uploaded);</p> </li> <li>asserts at least one classification exists;</li> <li>verifies that, for every transfer, the stored labels match the expected set.</li> </ul> </li> </ol>","text":""},{"location":"debt-positions-import-from-a-file/","title":"Debt positions import from a file","text":"<p>Tags: <code>@debt_positions_file</code></p>"},{"location":"debt-positions-import-from-a-file/#scenario-outline-debt-positions-are-created-from-an-ingestion-flow-file-version-eng-by-an-organization-interacting-with-gpd","title":"Scenario Outline: Debt positions are created from an ingestion flow file version -eng, by an organization interacting with GPD <p>Tags: <code>@&lt;csv_version&gt;</code> <code>@eng</code> <code>@gpd</code></p> <pre><code>Given organization interacting with GPD\nAnd debt positions &lt;identifiers&gt; with the installments configured as follows:\n  | identifier | po index | po type | installment seq | action |\n  | A | 1 | INSTALLMENTS | 1 | I |\n  | A | 1 | INSTALLMENTS | 2 | I |\n  | A | 2 | SINGLE_INSTALLMENT | 1 | I |\n  | B | 1 | INSTALLMENTS | 1 | I |\n  | B | 1 | INSTALLMENTS | 2 | I |\n  | C | 1 | SINGLE_INSTALLMENT | 1 | I |\nAnd debt positions &lt;identifiers&gt; inserted into an ingestion flow file with version &lt;csv_version&gt;\nWhen the organization uploads the debt positions file\nThen the ingestion file is processed correctly\n# (1)!\nAnd the debt positions &lt;identifiers&gt; are created in status UNPAID\n# (2)!\nAnd the notices of each debt positions are present in GPD archive in status valid\nAnd the checks of debt positions expiration are scheduled\n</code></pre> <ol> <li> <p>Checks that the debt-positions ingestion file was processed correctly. It verifies that:</p> <ul> <li>the file reaches <code>COMPLETED</code> (or <code>WARNING</code> when the organization has no generate-notice API key);</li> <li>the number of imported rows equals the rows sent and all were imported successfully;</li> <li>the <code>DEBT_POSITION_INGESTION_FLOW</code> workflow completes.</li> <li> <p>Checks that the debt positions imported from the file were created in the expected status:</p> </li> <li> <p>fetches the debt positions by ingestion-flow id and asserts the count matches the identifiers;</p> </li> <li>validates each created debt position against its request (fields, payment options, installments).</li> </ul> </li> </ol> <p>Examples</p>    identifiers csv_version     A B C 2_0-eng   A B C 2_0","text":""},{"location":"debt-positions-import-from-a-file/#scenario-outline-debt-positions-are-created-from-an-ingestion-flow-file-version-by-an-organization-interacting-with-gpd","title":"Scenario Outline: Debt positions are created from an ingestion flow file version , by an organization interacting with GPD <p>Tags: <code>@&lt;csv_version&gt;</code> <code>@it</code> <code>@gpd</code></p> <pre><code>Given organization interacting with GPD\nAnd debt positions &lt;identifiers&gt; with the installments configured as follows:\n  | identifier | po index | po type | installment seq | action |\n  | A | 1 | SINGLE_INSTALLMENT | 1 | I |\n  | B | 1 | SINGLE_INSTALLMENT | 1 | I |\n  | C | 1 | SINGLE_INSTALLMENT | 1 | I |\nAnd debt positions &lt;identifiers&gt; inserted into an ingestion flow file with version &lt;csv_version&gt;\nWhen the organization uploads the debt positions file\nThen the ingestion file is processed correctly\n# (1)!\nAnd the debt positions &lt;identifiers&gt; are created in status UNPAID\n# (2)!\nAnd the notices of each debt positions are present in GPD archive in status valid\nAnd the checks of debt positions expiration are scheduled\n</code></pre> <ol> <li> <p>Checks that the debt-positions ingestion file was processed correctly. It verifies that:</p> <ul> <li>the file reaches <code>COMPLETED</code> (or <code>WARNING</code> when the organization has no generate-notice API key);</li> <li>the number of imported rows equals the rows sent and all were imported successfully;</li> <li>the <code>DEBT_POSITION_INGESTION_FLOW</code> workflow completes.</li> <li> <p>Checks that the debt positions imported from the file were created in the expected status:</p> </li> <li> <p>fetches the debt positions by ingestion-flow id and asserts the count matches the identifiers;</p> </li> <li>validates each created debt position against its request (fields, payment options, installments).</li> </ul> </li> </ol> <p>Examples</p>    identifiers csv_version     A B C 1_4   A B C 1_3   A B C 1_2   A B C 1_1   A B C 1_0","text":""},{"location":"glossary/","title":"Steps glossary","text":"<p>Explains the steps that perform several checks internally. Steps that do a single, self-explanatory action are intentionally left out.</p>"},{"location":"glossary/#given","title":"Given","text":""},{"location":"glossary/#a-complex-debt-position-with-po_size-payment-options-created-by-organization-interacting-with-pagopa_interaction","title":"<code>a complex debt position with {po_size} payment options created by organization interacting with {pagopa_interaction}</code>","text":"<p>also: <code>a debt position {dp_identifier} with {po_size} payment option and {installments_size} installments created by organization interacting with {pagopa_interaction}</code></p> <p>Creates a complex debt position (several payment options with installments) in one step. It:</p> <ul> <li>gets the organization token and builds the payment options;</li> <li>creates the debt position and checks it becomes <code>UNPAID</code>;</li> <li>verifies presence in the GPD/ACA archive and the scheduled expiration workflow.</li> </ul>"},{"location":"glossary/#a-mixed-debt-position-created-by-sil-for-organization-interacting-with-pagopa_interaction-configured-as-follows","title":"<code>a mixed debt position created by SIL for organization interacting with {pagopa_interaction} configured as follows</code>","text":"<p>Creates a mixed debt position via SIL in one step. It:</p> <ul> <li>gets the SIL token and builds the mixed entity;</li> <li>sends it through SIL and retrieves the resulting debt positions by IUV;</li> <li>validates all data of generated debt positions (status, amounts, expiration workflow, ecc);</li> <li>verifies presence as <code>valid</code> in the GPD/ACA archive.</li> </ul>"},{"location":"glossary/#a-notification-created-for-the-single-installment-of-debt-position-dp_identifiers","title":"<code>a notification created for the single installment of debt position {dp_identifiers}</code>","text":"<p>also: <code>a notification created for the single installment of debt positions {dp_identifiers}</code>, <code>a notification created for the {installments_size} installments of debt positions {dp_identifiers}</code></p> <p>Creates a SEND notification for the selected installments. It:</p> <ul> <li>builds the notification request (recipients and pagoPA payments) for the given debt positions;</li> <li>gets a SEND token and posts the notification;</li> <li>asserts the call succeeds and the notification is created in status <code>WAITING</code>.</li> </ul>"},{"location":"glossary/#a-payment-reporting-with-outcome-code-9-has-been-successfully-processed-for-the-installment-created-outside-pu","title":"<code>a payment reporting with outcome code 9 has been successfully processed for the installment created outside PU</code>","text":"<p>For a debt position created outside PU, uploads a payment reporting with outcome code 9 and checks, in one step, that it is processed and that the debt position is created correctly.</p>"},{"location":"glossary/#a-payment-reporting-with-outcome-code-outcome_code-has-been-successfully-processed-for-the-installment","title":"<code>a payment reporting with outcome code {outcome_code} has been successfully processed for the installment</code>","text":"<p>Uploads a payment reporting and checks it is processed, in one step:</p> <ul> <li>uploads the file,</li> <li>checks the debt position becomes <code>REPORTED</code>,</li> <li>checks processing and (for outcome 9) that the receipt is created.</li> </ul>"},{"location":"glossary/#a-simple-debt-position-created-by-organization-interacting-with-pagopa_interaction","title":"<code>a simple debt position created by organization interacting with {pagopa_interaction}</code>","text":"<p>also: <code>a simple debt position of type {dp_type_org_code} created by organization interacting with {pagopa_interaction}</code>, <code>a simple debt position {dp_identifier} for citizen {citizen_identifier} created by organization interacting with {pagopa_interaction}</code></p> <p>Creates a simple, single-installment debt position in one step. It:</p> <ul> <li>gets the organization token and builds the debt position entity;</li> <li>creates it and checks it becomes <code>UNPAID</code>;</li> <li>verifies the notice is present as <code>valid</code> in the GPD/ACA archive;</li> <li>checks the expiration workflow is scheduled.</li> </ul>"},{"location":"glossary/#a-simple-debt-position-created-on-pagopa_interaction","title":"<code>a simple debt position created on {pagopa_interaction}</code>","text":"<p>Creates a simple debt position directly on GPD/ACA (bypassing PU creation):</p> <ul> <li>builds the entity and its transfer, generating IUV/NAV/IUPD;</li> <li>posts it to GPD/ACA and asserts the call succeeds;</li> <li>verifies the notice is present as <code>VALID</code> in the GPD/ACA archive.</li> </ul>"},{"location":"glossary/#a-simple-debt-position-with-balance-created-by-organization-interacting-with-pagopa_interaction","title":"<code>a simple debt position with balance created by organization interacting with {pagopa_interaction}</code>","text":"<p>Like <code>a simple debt position created by organization...</code>, but with a balance set to the installment; creates it and checks <code>UNPAID</code> status, presence in GPD/ACA and the scheduled expiration workflow.</p>"},{"location":"glossary/#the-previous-payment-of-installment-seq_num-of-payment-option-po_index","title":"<code>the previous payment of installment {seq_num} of payment option {po_index}</code>","text":"<p>Given that plays a full payment-and-reporting cycle for one installment and checks the outcome:</p> <ul> <li>pays the installment and checks the receipt is processed;</li> <li>checks the other payment options become <code>INVALID</code>;</li> <li>uploads the payment reporting and checks the installment becomes <code>REPORTED</code>;</li> <li>uploads the treasury file and checks it is processed;</li> <li>checks the payment option and debt position become <code>PARTIALLY_PAID</code>;</li> <li>checks the resulting classification labels.</li> </ul>"},{"location":"glossary/#the-successful-payment-of-the-installment","title":"<code>the successful payment of the installment</code>","text":"<p>Performs a full successful payment in one step:</p> <ul> <li>pays the installment,</li> <li>checks the debt position becomes <code>PAID</code></li> <li>checks the receipt is processed correctly.</li> </ul>"},{"location":"glossary/#the-successful-payment-of-the-installment-created-outside-pu","title":"<code>the successful payment of the installment created outside PU</code>","text":"<p>Like <code>the successful payment of the installment</code>, but pays the first installment of a debt position created outside PU, then checks the <code>PAID</code> status and that the receipt is processed.</p>"},{"location":"glossary/#when","title":"When","text":""},{"location":"glossary/#sil-creates-the-mixed-debt-position","title":"<code>SIL creates the mixed debt position</code>","text":"<p>Creates the mixed debt position through SIL (<code>paaSILInviaDovuti</code>) and asserts the SOAP outcome is <code>OK</code> with a URL to proceed for payment.</p>"},{"location":"glossary/#the-citizen-pays-the-installment-of-payment-option-po_index","title":"<code>the citizen pays the installment of payment option {po_index}</code>","text":"<p>also: <code>the citizen pays the installment {seq_num} of payment option {po_index}</code>, <code>the citizen {citizen_identifier} pays the installment of debt position {dp_identifier}</code>, <code>the citizen pays the installment {seq_num} of debt position {dp_identifier}</code></p> <p>Simulates the citizen paying the installment through the pagoPA node. It:</p> <ul> <li>verifies the payment notice (<code>verifyPaymentNotice</code>) and reads amount/due date;</li> <li>activates the payment notice (<code>activatePaymentNotice</code>) and gets a payment token;</li> <li>sends the payment outcome (<code>sendPaymentOutcome</code>), each call expected to return <code>OK</code></li> </ul>"},{"location":"glossary/#the-organization-creates-the-debt-position","title":"<code>the organization creates the debt position</code>","text":"<p>Creates the debt position and validates the outcome. It:</p> <ul> <li>posts the debt position and asserts the call succeeds;</li> <li>validates the whole created structure (payment options, installments, first transfer) in <code>TO_SYNC</code>;</li> <li>checks the organization's sync workflow (ACA/GPD) completes.</li> </ul>"},{"location":"glossary/#the-organization-requires-the-notification-to-be-uploaded-to-send","title":"<code>the organization requires the notification to be uploaded to SEND</code>","text":"<p>Uploads the notification and payment PDFs to SEND. It:</p> <ul> <li>uploads the notification file and one payment file per notified installment;</li> <li>reads the workflow id from the last upload;</li> <li>checks the notification moves to <code>IN_VALIDATION</code> with no IUN yet;</li> <li>waits for the upload workflow to complete.</li> </ul>"},{"location":"glossary/#then","title":"Then","text":""},{"location":"glossary/#3-debt-positions-having-installments-with-same-iuv-are-in-status-status-configured-as-follows","title":"<code>3 debt positions having installments with same IUV are in status {status} configured as follows</code>","text":"<p>Checks the debt positions generated from a mixed SIL flow (sharing the same IUV). For each configured row it:</p> <ul> <li>looks up the spontaneous-mixed or spontaneous-SIL debt position and asserts they exist as configured;</li> <li>validates their status, amounts, payment option and installments/transfers against the request.</li> </ul>"},{"location":"glossary/#send-has-set-a-notification-fee","title":"<code>SEND has set a notification fee</code>","text":"<p>Checks that SEND set a notification fee: reads the fee for each notified installment, asserts it is present and that all installments share the same fee.</p>"},{"location":"glossary/#the-amount-of-installment-of-debt-position-dp_identifier-is-increased-by-the-notification-fee","title":"<code>the amount of installment of debt position {dp_identifier} is increased by the notification fee</code>","text":"<p>also: <code>the amount of installment {seq_num} of debt position {dp_identifier} is increased by the notification fee</code></p> <p>Checks that the notified installment's amount was increased by the notification fee (both <code>notificationFeeCents</code> and <code>amountCents</code>).</p>"},{"location":"glossary/#the-assessment-classification-label-for-each-iud-is-label","title":"<code>the assessment classification label for each IUD is {label}</code>","text":"<p>Checks the assessment-detail classification labels for a mixed debt position. It:</p> <ul> <li>asserts the debt position with type ord MIXED has no assessment details/classification;</li> <li>for each mixed transfer, checks the <code>CLASSIFY_ASSESSMENT</code> workflow completes;</li> <li>verifies one assessment detail per balance section, each with the expected IUR, type org code and label.</li> </ul>"},{"location":"glossary/#the-assessment-detail-is-created-correctly-based-on-balance","title":"<code>the assessment detail is created correctly based on balance</code>","text":"<p>also: <code>the assessment detail is created correctly based on balance including {dptobc_type} section</code></p> <p>Checks that assessment details were created from the balance. It:</p> <ul> <li>retrieves the expected balance sections (optionally including a given DPTOBC section);</li> <li>checks the <code>CLASSIFY_ASSESSMENT</code> workflow completes;</li> <li>verifies one assessment detail per section with matching IUR, <code>PAID</code> label, amount and registry codes.</li> </ul>"},{"location":"glossary/#the-assessment-is-in-status-status","title":"<code>the assessment is in status {status}</code>","text":"<p>also: <code>the assessment related to debt position {dp_identifier} is in status {status}</code></p> <p>Checks that the assessment for the paid installment is in the expected status. It:</p> <ul> <li>reads the installment and its <code>sourceFlowName</code>;</li> <li>fetches the assessment by that name and asserts it exists and matches the status;</li> <li>checks the <code>CREATE_ASSESSMENT</code> workflow completes.</li> </ul>"},{"location":"glossary/#the-classification-labels-are-labels","title":"<code>the classification labels are {labels}</code>","text":"<p>also: <code>the classification labels for each transfer are {labels}</code>, <code>the classification labels related to payment reporting with outcome code {outcome_code} are {labels}</code></p> <p>Checks the classification labels produced for the installment. It:</p> <ul> <li>queries the classifications for today by IUV (and by IUF/IUR when payment reporting with outcome code specified is uploaded);</li> <li>asserts at least one classification exists;</li> <li>verifies that, for every transfer, the stored labels match the expected set.</li> </ul>"},{"location":"glossary/#the-debt-position-is-created-correctly","title":"<code>the debt position is created correctly</code>","text":"<p>Checks that the debt position was created correctly. It:</p> <ul> <li>looks it up by NAV and asserts exactly one is found;</li> <li>verifies its origin and, for receipt-file origin, that it is <code>PAID</code>;</li> <li>checks the installment IUV and, for pagopa-reporting origin, the outcome-9 remittance and anonymous debtor.</li> </ul>"},{"location":"glossary/#the-debt-positions-are-created-correctly-with-origin-debt_position_origin","title":"<code>the debt positions are created correctly with origin {debt_position_origin}</code>","text":"<p>Runs the <code>the debt position is created correctly</code> checks for every debt position imported from file.</p>"},{"location":"glossary/#the-debt-positions-identifiers-are-created-in-status-status","title":"<code>the debt positions {identifiers} are created in status {status}</code>","text":"<p>Checks that the debt positions imported from the file were created in the expected status:</p> <ul> <li>fetches the debt positions by ingestion-flow id and asserts the count matches the identifiers;</li> <li>validates each created debt position against its request (fields, payment options, installments).</li> </ul>"},{"location":"glossary/#the-duplicate-receipt-for-the-payment-reporting-with-outcome-code-outcome_code-is-created-correctly-with-origin-receipt_origin","title":"<code>the duplicate receipt for the payment reporting with outcome code {outcome_code} is created correctly with origin {receipt_origin}</code>","text":"<p>Checks that the duplicate receipt for the given payment-reporting flow (by outcome code) is created with the expected origin.</p>"},{"location":"glossary/#the-ingestion-file-is-processed-correctly","title":"<code>the ingestion file is processed correctly</code>","text":"<p>Checks that the debt-positions ingestion file was processed correctly. It verifies that:</p> <ul> <li>the file reaches <code>COMPLETED</code> (or <code>WARNING</code> when the organization has no generate-notice API key);</li> <li>the number of imported rows equals the rows sent and all were imported successfully;</li> <li>the <code>DEBT_POSITION_INGESTION_FLOW</code> workflow completes.</li> </ul>"},{"location":"glossary/#the-mixed-debt-position-and-technical-ones-are-in-status-status","title":"<code>the mixed debt position and technical ones are in status {status}</code>","text":"<p>Checks that the mixed debt position and its technical (spontaneous-mixed) ones are all in the expected status.</p>"},{"location":"glossary/#the-notification-is-in-status-status-and-the-iun-is-assigned-to-the-installment","title":"<code>the notification is in status {status} and the IUN is assigned to the installment</code>","text":"<p>also: <code>the notification is in status {status} and the IUN is assigned to all installments</code></p> <p>Checks that the notification is valid and the IUN is assigned. It:</p> <ul> <li>polls SEND until the notification is valid and asserts it has an IUN;</li> <li>verifies that every notified installment carries that same IUN.</li> </ul>"},{"location":"glossary/#the-payment-reporting-is-processed-correctly","title":"<code>the payment reporting is processed correctly</code>","text":"<p>Checks that the payment reporting file was processed correctly. It verifies that:</p> <ul> <li>the file reaches status <code>COMPLETED</code>;</li> <li>the <code>PAYMENTS_REPORTING_INGESTION</code> workflow completes;</li> <li>the <code>TRANSFER_CLASSIFICATION</code> workflow completes for each transfer;</li> <li>the installment now has its <code>IUF</code> set.</li> </ul>"},{"location":"glossary/#the-payment-reporting-with-outcome-code-9-is-processed-correctly","title":"<code>the payment reporting with outcome code 9 is processed correctly</code>","text":"<p>Checks that a payment reporting with outcome code 9 was processed correctly. It verifies that:</p> <ul> <li>the file reaches status <code>COMPLETED</code>;</li> <li>the <code>PAYMENTS_REPORTING_INGESTION</code> workflow completes;</li> <li>the <code>TRANSFER_CLASSIFICATION</code> workflow completes.</li> </ul>"},{"location":"glossary/#the-receipt-is-created-correctly-with-origin-receipt_origin","title":"<code>the receipt is created correctly with origin {receipt_origin}</code>","text":"<p>Checks that the receipt for the paid installment was created with the expected origin. It verifies that:</p> <ul> <li>the receipt exists (looked up by IUV/IUR, or by IUR when it is a duplicate);</li> <li>its <code>receiptOrigin</code> matches the expected one and it is linked to the installment (<code>receiptId</code>);</li> <li>the <code>TRANSFER_CLASSIFICATION</code> workflow completes for each transfer;</li> <li>the <code>IUD_CLASSIFICATION</code> workflow completes.</li> </ul>"},{"location":"glossary/#the-receipt-is-processed-correctly","title":"<code>the receipt is processed correctly</code>","text":"<p>also: <code>the receipt of debt position {dp_identifier} is processed correctly</code></p> <p>Checks that the pagoPA receipt has been fully processed. It verifies that:</p> <ul> <li>the receipt file <code>RT_&lt;nav&gt;.xml</code> reaches status <code>COMPLETED</code>;</li> <li>the paid installment now has both <code>IUR</code> and <code>receiptId</code> set;</li> <li>the async <code>TRANSFER_CLASSIFICATION</code> workflow completes;</li> <li>the async <code>IUD_CLASSIFICATION</code> workflow completes.</li> </ul>"},{"location":"glossary/#the-receipts-are-created-correctly-with-origin-receipt_origin","title":"<code>the receipts are created correctly with origin {receipt_origin}</code>","text":"<p>Runs the <code>the receipt is created correctly</code> checks for every receipt imported from the file.</p>"},{"location":"glossary/#the-receipts-file-is-processed-correctly","title":"<code>the receipts file is processed correctly</code>","text":"<p>Checks that the receipts file was processed correctly. It verifies that:</p> <ul> <li>the file reaches status <code>COMPLETED</code>;</li> <li>the number of imported rows equals the rows sent and all were imported successfully;</li> <li>the <code>RECEIPT_INGESTION_FLOW</code> workflow completes.</li> </ul>"},{"location":"glossary/#the-treasury-is-processed-correctly","title":"<code>the treasury is processed correctly</code>","text":"<p>Checks that the treasury OPI file was processed correctly. It verifies that:</p> <ul> <li>the file reaches status <code>COMPLETED</code>;</li> <li>the <code>TREASURY_OPI_INGESTION</code> workflow completes;</li> <li>the <code>TRANSFER_CLASSIFICATION</code> workflow completes for each transfer;</li> <li>the <code>IUF_CLASSIFICATION</code> workflow completes.</li> </ul>"},{"location":"mixed-debt-position-management-by-sil/","title":"Mixed debt position management by SIL","text":"<p>Tags: <code>@sil_debt_positions</code></p>"},{"location":"mixed-debt-position-management-by-sil/#scenario-a-mixed-debt-position-is-created-by-a-sil-for-an-organization-interacting-with-gpd","title":"Scenario: A mixed debt position is created by a SIL for an organization interacting with GPD","text":"<p>Tags: <code>@debt_position_mixed</code></p> <pre><code>Given a SIL acting on behalf of an organization interacting with GPD\nAnd a new mixed debt position configured as follows:\n  | transfer index | type org | amount |\n  | 1 | FEATURE_TEST | 34.00 |\n  | 2 | FEATURE_TEST_2 | 48.00 |\n  | 3 | FEATURE_TEST | 68.00 |\nWhen SIL creates the mixed debt position\n# (1)!\nThen 3 debt positions having installments with same IUV are in status unpaid configured as follows:\n  | origin | type org | total installments | total amount | transfers index |\n  | SPONTANEOUS_MIXED | FEATURE_TEST | 2 | 102.00 | 1 3 |\n  | SPONTANEOUS_MIXED | FEATURE_TEST_2 | 1 | 48.00 | 2 |\n  | SPONTANEOUS_SIL | MIXED | 1 | 150.00 | 1 2 3 |\nAnd the notice is present in GPD archive in status valid\nAnd the check of debt position expiration is scheduled\n</code></pre> <ol> <li>Creates the mixed debt position through SIL (<code>paaSILInviaDovuti</code>) and asserts the SOAP outcome is <code>OK</code> with a URL to proceed for payment.</li> </ol>"},{"location":"mixed-debt-position-management-by-sil/#scenario-a-mixed-debt-position-created-by-a-sil-on-gpd-is-reported-after-payment-payment-reporting-and-treasury","title":"Scenario: A mixed debt position created by a SIL on GPD is reported after payment, payment reporting and treasury","text":"<p>Tags: <code>@debt_position_mixed_classification</code></p> <pre><code>Given a mixed debt position created by SIL for organization interacting with GPD configured as follows:\n  | transfer index | type org | amount |\n  | 1 | FEATURE_TEST | 43.00 |\n  | 2 | FEATURE_TEST_2 | 30.00 |\n  | 3 | FEATURE_TEST | 77.00 |\nWhen the citizen pays the installment of mixed debt position\nThen the receipt is processed correctly\n# (1)!\nAnd the mixed debt position and technical ones are in status paid\n# (2)!\nAnd the check of mixed debt position expiration is canceled\nAnd the classification labels for each transfer are RT_NO_IUF, RT_NO_IUD\n# (3)!\nAnd the assessment classification label for each IUD is PAID\n# (4)!\nWhen the organization uploads the payment reporting file about mixed installment\nThen the payment reporting is processed correctly\n# (5)!\nAnd the mixed debt position and technical ones are in status reported\n# (6)!\nAnd the classification labels are RT_IUF, IUF_NO_TES, RT_NO_IUD\n# (7)!\nAnd the assessment classification label for each IUD is REPORTED\n# (8)!\nWhen the organization uploads the treasury file with amount of 150 euros\nThen the treasury is processed correctly\n# (9)!\nAnd the mixed debt position and technical ones are in status reported\n# (10)!\nAnd the classification labels are RT_IUF, RT_IUF_TES, RT_NO_IUD\n# (11)!\nAnd the assessment classification label for each IUD is CASHED\n# (12)!\n</code></pre> <ol> <li> <p>Checks that the pagoPA receipt has been fully processed. It verifies that:</p> <ul> <li>the receipt file <code>RT_&lt;nav&gt;.xml</code> reaches status <code>COMPLETED</code>;</li> <li>the paid installment now has both <code>IUR</code> and <code>receiptId</code> set;</li> <li>the async <code>TRANSFER_CLASSIFICATION</code> workflow completes;</li> <li>the async <code>IUD_CLASSIFICATION</code> workflow completes.</li> <li>Checks that the mixed debt position and its technical (spontaneous-mixed) ones are all in the expected status.</li> <li> <p>Checks the classification labels produced for the installment. It:</p> </li> <li> <p>queries the classifications for today by IUV (and by IUF/IUR when payment reporting with outcome code specified is uploaded);</p> </li> <li>asserts at least one classification exists;</li> <li>verifies that, for every transfer, the stored labels match the expected set.</li> <li> <p>Checks the assessment-detail classification labels for a mixed debt position. It:</p> </li> <li> <p>asserts the debt position with type ord MIXED has no assessment details/classification;</p> </li> <li>for each mixed transfer, checks the <code>CLASSIFY_ASSESSMENT</code> workflow completes;</li> <li>verifies one assessment detail per balance section, each with the expected IUR, type org code and label.</li> <li> <p>Checks that the payment reporting file was processed correctly. It verifies that:</p> </li> <li> <p>the file reaches status <code>COMPLETED</code>;</p> </li> <li>the <code>PAYMENTS_REPORTING_INGESTION</code> workflow completes;</li> <li>the <code>TRANSFER_CLASSIFICATION</code> workflow completes for each transfer;</li> <li>the installment now has its <code>IUF</code> set.</li> <li>Checks that the mixed debt position and its technical (spontaneous-mixed) ones are all in the expected status.</li> <li> <p>Checks the classification labels produced for the installment. It:</p> </li> <li> <p>queries the classifications for today by IUV (and by IUF/IUR when payment reporting with outcome code specified is uploaded);</p> </li> <li>asserts at least one classification exists;</li> <li>verifies that, for every transfer, the stored labels match the expected set.</li> <li> <p>Checks the assessment-detail classification labels for a mixed debt position. It:</p> </li> <li> <p>asserts the debt position with type ord MIXED has no assessment details/classification;</p> </li> <li>for each mixed transfer, checks the <code>CLASSIFY_ASSESSMENT</code> workflow completes;</li> <li>verifies one assessment detail per balance section, each with the expected IUR, type org code and label.</li> <li> <p>Checks that the treasury OPI file was processed correctly. It verifies that:</p> </li> <li> <p>the file reaches status <code>COMPLETED</code>;</p> </li> <li>the <code>TREASURY_OPI_INGESTION</code> workflow completes;</li> <li>the <code>TRANSFER_CLASSIFICATION</code> workflow completes for each transfer;</li> <li>the <code>IUF_CLASSIFICATION</code> workflow completes.</li> <li>Checks that the mixed debt position and its technical (spontaneous-mixed) ones are all in the expected status.</li> <li> <p>Checks the classification labels produced for the installment. It:</p> </li> <li> <p>queries the classifications for today by IUV (and by IUF/IUR when payment reporting with outcome code specified is uploaded);</p> </li> <li>asserts at least one classification exists;</li> <li>verifies that, for every transfer, the stored labels match the expected set.</li> <li> <p>Checks the assessment-detail classification labels for a mixed debt position. It:</p> </li> <li> <p>asserts the debt position with type ord MIXED has no assessment details/classification;</p> </li> <li>for each mixed transfer, checks the <code>CLASSIFY_ASSESSMENT</code> workflow completes;</li> <li>verifies one assessment detail per balance section, each with the expected IUR, type org code and label.</li> </ul> </li> </ol>"},{"location":"receipts-import-from-a-file/","title":"Receipts import from a file","text":"<p>Tags: <code>@receipts_file</code></p>"},{"location":"receipts-import-from-a-file/#scenario-outline-missing-debt-positions-are-created-by-importing-a-receipts-file-with-an-organization-interacting-with","title":"Scenario Outline: Missing debt positions are created by importing a receipts file, with an organization interacting with  <pre><code>Given organization interacting with &lt;interaction&gt;\nAnd receipts of non-existent debt positions inserted into an ingestion flow file with version 1_3\nWhen the organization uploads the receipts file\nThen the receipts file is processed correctly\n# (1)!\nAnd the debt positions are created correctly with origin receipt_file\n# (2)!\nAnd the receipts are created correctly with origin receipt_file\n# (3)!\n</code></pre> <ol> <li> <p>Checks that the receipts file was processed correctly. It verifies that:</p> <ul> <li>the file reaches status <code>COMPLETED</code>;</li> <li>the number of imported rows equals the rows sent and all were imported successfully;</li> <li>the <code>RECEIPT_INGESTION_FLOW</code> workflow completes.</li> <li>Runs the <code>the debt position is created correctly</code> checks for every debt position imported from file.</li> <li>Runs the <code>the receipt is created correctly</code> checks for every receipt imported from the file.</li> </ul> </li> </ol> <p>Examples</p>    interaction     GPD    <p>Examples</p>    interaction     ACA","text":""},{"location":"receipts-import-from-a-file/#scenario-outline-a-debt-position-is-updated-by-importing-a-receipts-file-with-an-organization-interacting-with","title":"Scenario Outline: A debt position is updated by importing a receipts file, with an organization interacting with  <pre><code>Given a simple debt position created by organization interacting with &lt;interaction&gt;\n# (1)!\nAnd a receipt of a debt position inserted into an ingestion flow file with version 1_3\nWhen the organization uploads the receipts file\nThen the receipts file is processed correctly\n# (2)!\nAnd the receipt is created correctly with origin receipt_file\n# (3)!\nAnd the installment of payment option 1 is in status paid\nAnd the debt position is in status paid\n</code></pre> <ol> <li> <p>Creates a simple, single-installment debt position in one step. It:</p> <ul> <li>gets the organization token and builds the debt position entity;</li> <li>creates it and checks it becomes <code>UNPAID</code>;</li> <li>verifies the notice is present as <code>valid</code> in the GPD/ACA archive;</li> <li>checks the expiration workflow is scheduled.</li> <li> <p>Checks that the receipts file was processed correctly. It verifies that:</p> </li> <li> <p>the file reaches status <code>COMPLETED</code>;</p> </li> <li>the number of imported rows equals the rows sent and all were imported successfully;</li> <li>the <code>RECEIPT_INGESTION_FLOW</code> workflow completes.</li> <li> <p>Checks that the receipt for the paid installment was created with the expected origin. It verifies that:</p> </li> <li> <p>the receipt exists (looked up by IUV/IUR, or by IUR when it is a duplicate);</p> </li> <li>its <code>receiptOrigin</code> matches the expected one and it is linked to the installment (<code>receiptId</code>);</li> <li>the <code>TRANSFER_CLASSIFICATION</code> workflow completes for each transfer;</li> <li>the <code>IUD_CLASSIFICATION</code> workflow completes.</li> </ul> </li> </ol> <p>Examples</p>    interaction     GPD    <p>Examples</p>    interaction     ACA","text":""}]}