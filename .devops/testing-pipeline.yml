trigger:
  branches:
    include:
      - main

schedules:
  - cron: "0 6 * * 1-5" # Runs from monday to friday
    displayName: Scheduled run
    branches:
      include:
        - main
    always: true

parameters:
  - name: env
    displayName: Target Environment
    type: string
    default: dev
    values:
      - dev
      - uat

  - name: tag
    displayName: Target tests tag (use unquoted "all" to run every un-skipped test)
    type: string
    default: 'all'
    values:
      - all
      - aca
      - gpd
      - debt_positions
      - payment
      - classification
      - debt_positions_file
      - receipts_file
      #- send
      - classification_outcome9
      - assessments
      - cie_debt_positions
      - sil_debt_positions
      - cie_demand_payment_notice

variables:
  # Python version: 3.x
  pythonVersion: '3.x'
  # Folder name of this sub-repository
  working-dir: '.'
  # Project root folder
  projectRoot: $(System.DefaultWorkingDirectory)/$(working-dir)
  # Key Vault config
  ${{ if eq(parameters.env, 'dev') }}:
    azureServiceConnection: $(DEV_AZURE_SERVICE_CONNECTION_NAME)
    keyVaultName: $(DEV_KV_NAME)
    secretName: $(DEV_FEATURE_SECRET_NAME)

  ${{ if eq(parameters.env, 'uat') }}:
    azureServiceConnection: $(UAT_AZURE_SERVICE_CONNECTION_NAME)
    keyVaultName: $(UAT_KV_NAME)
    secretName: $(UAT_FEATURE_SECRET_NAME)

pool:
  vmImage: 'ubuntu-22.04'

stages:
  - stage: Feature_tests
    dependsOn: [ ]
    pool:
      name: 'p4pa-${{ parameters.env }}-linux-app'
    jobs:
      - job: "${{ upper(parameters.env) }}_Run_all_feature_tests"
        condition: and(succeeded(), or(eq('${{ parameters.tag }}', ''), eq('${{ parameters.tag }}', 'all')))
        strategy:
          maxParallel: 3
          matrix:
            debt positions:
              current_tag: 'debt_positions'
            classification:
              current_tag: 'classification'
            debt_positions_file:
              current_tag: 'debt_positions_file'
            receipts_file:
              current_tag: 'receipts_file'
            #send:
            #  current_tag: 'send'
            assessments:
              current_tag: 'assessments'
            sil_debt_positions:
              current_tag: 'sil_debt_positions'
            cie_debt_positions:
              current_tag: 'cie_debt_positions'
            cie_demand_payment_notice:
              current_tag: 'cie_demand_payment_notice'
        steps:
          - checkout: self
          - template: template\test-run.yml
            parameters:
              env: ${{ parameters.env }}
              tag: '$(current_tag)'
              azureServiceConnection: $(azureServiceConnection)
              keyVaultName: $(keyVaultName)
              secretName: $(secretName)
      - job: "${{ upper(parameters.env) }}_Run_feature_tests_by_tag"
        condition: and(succeeded(), and( not(eq('${{ parameters.tag }}', '')), not(eq('${{ parameters.tag }}', 'all'))))
        steps:
          - checkout: self
          - template: template\test-run.yml
            parameters:
              env: ${{ parameters.env }}
              tag: ${{ parameters.tag }}
              azureServiceConnection: $(azureServiceConnection)
              keyVaultName: $(keyVaultName)
              secretName: $(secretName)
